import 'package:flutter/material.dart';

class History extends StatelessWidget {
  History({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Ill populate this after creting the clses"),
    );
  }
}
